@model IEnumerable<Domain.GEN.AccountingAuxiliary>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AccountingAccount.Code)

        </th>
        <th>
            @Html.DisplayNameFor(model => model.Code)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AccountingAccount.Code)
            -
            @Html.DisplayFor(modelItem => item.AccountingAccount.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Code)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AccountingAuxiliaryId }) |
            @Html.ActionLink("Details", "Details", new { id=item.AccountingAuxiliaryId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AccountingAuxiliaryId })
        </td>
    </tr>
}

</table>
