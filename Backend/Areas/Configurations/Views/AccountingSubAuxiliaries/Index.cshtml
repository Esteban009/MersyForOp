@model IEnumerable<Domain.GEN.AccountingSubAuxiliary>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AccountingAuxiliary.Code)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Code)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AccountingAuxiliary.Code)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Code)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AccountingSubAuxiliaryId }) |
            @Html.ActionLink("Details", "Details", new { id=item.AccountingSubAuxiliaryId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AccountingSubAuxiliaryId })
        </td>
    </tr>
}

</table>
